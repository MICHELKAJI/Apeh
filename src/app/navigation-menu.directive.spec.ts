import { NavigationMenuDirective } from './navigation-menu.directive';

describe('NavigationMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new NavigationMenuDirective();
    expect(directive).toBeTruthy();
  });
});
